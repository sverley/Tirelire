{
  "name": "tirelire",
  "private": true,
  "version": "0.1.0",
  "description": "Comptes de la famille : enveloppes, provisions, plan de virements et rapprochement bancaire.",
  "scripts": {
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "dev": "pnpm --filter @tirelire/web dev",
    "typecheck": "pnpm -r typecheck",
    "prepare": "simple-git-hooks"
  },
  "packageManager": "pnpm@10.28.0",
  "devDependencies": {
    "simple-git-hooks": "^2.14.0"
  },
  "simple-git-hooks": {
    "pre-commit": "pnpm -r typecheck && pnpm --filter @tirelire/core test && pnpm --filter @tirelire/gardes test",
    "pre-push": "pnpm build"
  }
}
